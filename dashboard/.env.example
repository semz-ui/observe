# The NestJS API. Only ever read server-side: /api/* and /v1/stats send no CORS
# headers, so every dashboard read goes through a server component or a route
# handler rather than the browser. See server/docs/phase-9-dashboard.md.
OBSERVE_API_URL=http://localhost:3000
